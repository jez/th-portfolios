extends base
mixin thumbnail(company)
    .thumbnail
        img(src='/images/' + company +'.svg')
        .caption
            form(id=company)
                .form-group
                    input.form-control(placeholder="Access code")
                .form-horizontal
                    .form-group
                        .col-sm-5
                            label.pull-right.control-label Download as...
                        .col-sm-7
                            .btn-group.pull-left
                                button.btn.btn-default csv
                                button.btn.btn-default bash
                                button.btn.btn-default json
block header
    link(rel="stylesheet" type="text/css" href="/css/index.css")
    style.
        img {
            height: 121px !important;
        }
block content
    .container
        .jumbotron#title
            h1.text-center TartanHacks 
                small Resume Downloader
        p.text-center.lead Please select your company and enter your access code to download the resume data of those who expressed interest in your company specifically. 
        p.text-center You can download it as either a 
            code .csv
            | , 
            code .sh
            | , or 
            code .json
            |  file. The bash script is only for downloading PDFs coupled to andrewids, so it's best to also grab the csv file too. 
        p.text-center If none of these formats work for you, please let us know so that we can accomodate you.
        hr
        .row
            .col-md-4
                +thumbnail('a16z')
            .col-md-4
                +thumbnail('aws')
            .col-md-4
                +thumbnail('apple')
        .row
            .col-md-4
                +thumbnail('bloomberg')
            .col-md-4
                +thumbnail('dropbox')
            .col-md-4
                +thumbnail('facebook')
        .row
            .col-md-4.col-md-offset-4
                +thumbnail('microsoft')
        hr
        h1.text-center Or...
        p.lead.text-center Download all resumes in one of the following formats
        .row
            .col-md-2.col-md-offset-3
                a.center-block.btn.btn-primary.btn-lg(href="/all/resumes.csv") csv
            .col-md-2
                a.center-block.btn.btn-primary.btn-lg(href="/all/resumes.sh") bash
            .col-md-2
                a.center-block.btn.btn-primary.btn-lg(href="/all/resumes.json") json
        hr
block script
    script(src="/js/resumes.js")
